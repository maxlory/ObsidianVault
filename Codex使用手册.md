在codex桌面版中按下 command + J 调出终端

在codex桌面版中，如果将之前的问题A修改成现在的问题B，那么问题A的上下文不会占用问题B的上下文，类似于fork功能：从一个分支跳转到另一个分支
但是只能修改最近的问题，之前的问题无法修改
可以从现在的问题B的分支跳转回问题A的分支

在codex桌面版中，fork按钮在codex的回复部分
fork可以将会话会滚到fork的所选消息位置，但是只回滚会话，不回滚代码
分为两个：
1. fork into local：代码保存到本地文件夹中
2. fork into new worktree：代码保存到新的文件夹中

/side 类似于Claude code 的/btw 的功能