区块链本质上是一种==特殊类型的数据库==，有时也称为<font color="#92d050">分布式账本</font>
区块链是**最常见的**分布式账本（很多人共同保存的“连续账本”）技术系统之一，因此，这两个术语经常交替使用
区块链——>分布式账本，但是区块链≠分布式账本
区块链能够存储信息，但是存储的信息必须遵循特殊规则
1. 能够添加数据（区块链需要不断记录新的交易或事件，所以允许在末尾追加新区块）
2. 不支持修改、删除数据
	1. 每个区块都会根据自身内容生成一个“数字指纹”，也就是哈希值。<font color="#92d050">只要旧数据改变，哈希值也会改变，后面的区块就无法继续正确连接</font>。
	2. 账本副本分散保存在很多节点上。想修改旧数据，不仅要重新计算后续区块，还要<font color="#92d050">让大多数节点接受修改</font>，成本非常高。
	注意：旧数据并非无法改变，只是成本很高、难度很大
3. 每条数据必须指向前一条数据
区块是遵循特定格式的一组信息
新数据只能作为区块的一部分添加至区块链中
因此，区块的集合构成了区块链数据库
但区块链的独特之处在于
每个区块都建立在前一个区块之上
每个单独的区块都包括部分信息
这些信息引用了前一个区块
区块具体如何连接呢？

哈希运算是区块链技术的底层逻辑
==哈希运算是指使用任意长度的输入数据，创建固定长度的字符串==。
	如何实现：将输入数据按照“位置和固定长度”机械地切分，输出是固定的长度
这一过程通过一种数学计算完成，称为哈希函数
我们可以使用各种长度不一的输入，例如文本、图像、视频等；但是输出还是哈希值，始终具有可预测的长度
哈希值的长度取决于所使用的哈希运算法则。
简而言之，哈希运算可以获取任意长度的数据；然后使用函数进行处理，生成固定长度的哈希值

在区块链技术中，每个区块都有专属的哈希值，用作唯一标识
每次创建新区块时，其哈希值必须根据前一个区块的哈希值生成。所以可以说，区块通过哈希值相连
这就导致每个区块的哈希值都是唯一的。因为几乎无法找到两条产生相同结果的路径（前置区块的哈希值不同）
哈希值冲突：两个不同的输入生成了相同的哈希值输出的情况。
抵抗哈希值冲突是防止区块链数据被篡改的关键
区块链的底层逻辑导致区块链几乎不可能被修改，自然就不存在需要人人都信任的中心化机构来控制系统运作

### 区块链技术的特点
安全性和冗余性：一个区块链可拥有无数用户，每位用户都可存储专属副本并与他人交流。即使多人离线，这个网络也能正常运行
不可篡改性：将区块添加至区块链后，即可认为该区块是永久性的。这还意味着去信任化和透明度
去信任化和透明度：不需要相信任何人，每个人都有专属的区块链副本，他们可以查看并验证其中存储的所有数据
去中心化：信息并非集中在单一的中央服务器上，而是存储在分布式计算机（节点）网络中。像比特币这样的去中心化网络在抵御攻击方面具有强大的能力
效率：区块链无需依赖中间机构，交易处理速度更快，成本更低，交易几乎实时完成。

### 区块链的工作原理
拥有一份用户间每笔交易的公开明细文件，对于您的银行而言并没有多大意义
由于其中心化客户端服务器模式，他们可以私下验证交易
	这句话什么意思：重点在于公开
	中心化服务器会自行检查账号是否对应、密码是否正确、余额是否充足的信息，因为这些信息都是由中心化服务器存储、修改的
	缺点：透明度低、可篡改、安全性低
区块链是公共数据库，人人都必须能够查看交易，并确认其有效性
加密和解密使用相同的密钥
公钥与私钥
一个是需要保密的私钥、另一个是可与他人分享的公钥
使用私钥可以生成相应的公钥，但是反之行不通
私钥持有者可编写消息，并使用私钥进行数字签名
对比公钥与签名后，即可安全地认为消息由私钥持有者编写

### 挖矿是什么
用户进行 BTC 交易时，会将其广播到网络中，由称为“矿工”的节点进行验证。挖矿是指验证交易并将交易记录至区块链数据库（账本）的过程。
首位成功解决难题的矿工可将新的交易区块添加至区块链中，然后获得新的比特币作为奖励。

### 加密货币的价值
比特币的使用价值：
	不必依赖任何中间机构，即可在全世界**快速转移**大量财富
	去中心的特性使比特币具有**规避中央机构监管**的能力。同时，比特币的安全性很高，因为网络中没有任何一个节点能够单凭个人意愿做决定
	**分散价值**：比特币没有单一的“事实发布者”，而是由分散的节点各自验证同一套规则，并对一份有效账本达成共识。节点越分散，系统通常越难被控制，越不会因为某些节点的问题而总体出问题
	**信任体系价值**：比特币的用户不需要彼此信任，只需要信任比特币技术。这项技术久经考验，可靠性和安全性值得信赖，源代码向所有人开放
	稀缺性价值：比特币的供应量有限，上限恒定为2100万枚


### Web3
去中心化的、开放的、以用户为中心的
读写和所有权网络
web3 旨在使用户移至去中心化平台，更好地控制个人数据
享受更高的隐私性，并通过共享数据或内容而获得奖励
用户真正成为内容所有者，而不仅仅是内容使用者
NFT：可以让用户成为数据和创作的所有者

Web 1.0：只能读，无法交互——仅能显示信息，用户无法更改数据或上传自己的数据。静态网页
Web 2.0：能读写、交互——