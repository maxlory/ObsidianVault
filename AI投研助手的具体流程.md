用户输入问题——>理解问题——>研究计划——>数据采集——>事实抽取——>财务计算——>多空辩论——>报告与审查
所有节点共享一个文件夹，即 `ResearchState`
节点不会相互联系，而是：
- 从 State 读取自己需要的数据
- 执行固定任务
- 把结果写回 State
- LangGraph 根据边决定下一个节点

#### 第零步：运行初始化
节点做了什么：每次用户发起研究，都创建一个独立的研究任务。
	产生一个新的任务ID
	同时初始化一份空的 `ResearchState`
输入：schema
输出：schema（==应该要将整个流程的schema都确定下来==）
怎么实现：普通 Python 代码，不需要 LLM，也不需要 Agent（初始化任务 ID、用户 ID、研究时点等内容）
为什么需要：未来一个研究任务可能运行几分钟甚至几十分钟。程序中断后，LangGraph 要依靠 `thread_id` 和 checkpoint 找回这次研究的 State。（必须在计划设计的时候就安排好出问题时的解决方案）

#### 第一步：understand_request
节点做了什么：只负责回答——用户到底想研究什么？（理解问题，为后续节点拆解问题做准备）
输入：用户研究的问题
输出：schema（研究主题、证券代码、研究时点等）
怎么实现：只使用prompt调用一次LLM
	为什么不用agent：该节点在原则上没有调用外部工具。
	不需要进行下一步决策与规划、不需要反复搜索、只需要完成一次“自然语言转结构化数据”（将自然语言任务输入转化成schema输出）
兜底方案：如果用户提供信息不完整/有歧义怎么办？

#### 第二步：build_research_plan
节点做了什么：为了回答这个研究问题，后面到底需要收集什么？（把问题拆成可执行任务清单。）
输入：schema（问题的拆解+数据源能力）
输出：schema（具体执行清单）
怎么实现：结构化的LLM 调用，使用代码门禁检查计划输出是否合理
	为什么不用agent：该节点在原则上没有调用外部工具。它只需要知道系统“拥有什么工具”，从而进行预计使用什么工具的安排。类似于ReAct中的reasoning环节
	==为什么不让 Research Agent 同时兼顾计划的制定与实现==：
	1. 计划被实现的细节淹没，导致目标迷失
	2. 不知道什么时候算完成（没有明确的完成目标）
	3. agent可能边做边改方向（防止上下文污染）
	4. 方便用户审查和干预
	5. 搜索预算容易失控


节点做了什么：
输入：
输出：
怎么实现：


节点做了什么：
输入：
输出：
怎么实现：


节点做了什么：
输入：
输出：
怎么实现：