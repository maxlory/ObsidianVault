在ReAct循环中，从API视角看到的上下文包括：静态前缀+动态轨迹
静态前缀：系统提示词+工具定义
动态轨迹：模型回复（对话历史+思考过程+工具调用请求）+用户信息+工具执行结果

对于大模型API内容的详解：消息列表（messages）
列表中的消息根据角色（role）分类，模型根据角色理解消息的含义和来源
system：系统提示词
user：用户消息
assistance：模型回复
tool：工具执行结果（不是工具定义）
工具定义（tools）作为请求的独立字段（而非消息），告诉模型有哪些工具可以使用、每个工具接受什么参数。
“四种消息角色 + `tools` 字段”恰好覆盖第一章所说的五个上下文组成部分

模型负责决策（调用什么工具、传什么参数），Agent 框架负责执行（实际调用 API、运行代码）

Chat Template是将API层面的结构化消息转换成模型能理解的线性token流
如何实现：用特殊标记（如 `<|im_start|>system`、`<|im_end|>`）划分每条消息的边界和角色。——类似于XML标签

KV Cache的本质：注意力机制——当前词在阅读上下文时，主动判断“哪些词对我最重要”，然后重点参考这些词。
生成的新token需要经过注意力机制计算后得出。KV Cache使得之前生成过的token的KV值不需要重新计算（但是每个新 token 的注意力计算仍要遍历全部缓存的 K、V，这会导致计算量随上下文长度线性增长）

**KV Cache** 是<mark style="background:#d3f8b6">模型内部的优化——在一次推理过程中，缓存已计算的 token 的键值对，避免重复计算</mark>
**Prompt Cache** 则是 <mark style="background:#d3f8b6">API 服务层的优化——跨多次 API 请求之间，缓存相同前缀的计算结果</mark>
KV Cache 加速单次请求内的 token 生成，Prompt Cache 减少跨请求的重复计算成本。

#### 提示词工程：
1. 注意语气和风格的设计，其深刻影响用户体验
2. 使用 XML 标签，为文本内容划分结构和边界
3. 针对人类降低认知负担的方法，对大语言模型同样有效——用户觉得提示词设计得好的方法，对LLM也有效
4. 业务规则细化——==这是未来产品或者说AI产品的核心竞争力==
5. few-shot examples：让模型临时学会例子——需要注意的是，对于模型本来就擅长、规则又容易说清的任务，示例只是浪费 token
	1. 示例的位置：放在系统提示词中，成为静态前缀的一部分，对所有请求生效；伪造一组 user/assistant 消息放在首轮对话位置，按会话类型选用不同的示例
	2. 示例对 KV Cache 前缀稳定性的影响：示例处于上下文靠前的位置，只能为每类任务准备固定的示例集，而不是按请求动态检索“最相关”的示例
	3. 示例数量不是重点，能够覆盖边界情况才是重点

使用消融实验验证提示工程各要素的贡献：
方法论：核心是控制变量法——设定一个基线配置（语气与风格、提示词组织形式、工具描述），逐个修改不同的系统组件来研究其影响
语气与风格：风格显著改变了表达方式，但对任务完成率的影响相对有限
提示词组织形式：提示词结构散乱后，任务成功率明显下降——对人类友好的信息组织形式对模型同样友好
工具描述：只保留工具定义，但是移除具体工具功能描述，工具调用错误率急剧上升
==方法论：当agent表现不佳时，最好的方法是采取消融实验，检查到底哪个部分对agent表现的影响最大==

#### 提示注入
定义：上下文被外部输入劫持
由于agent拥有工具调用的能力，所以一旦agent被劫持，可能导致产生不可逆、破环形强的后果；提示注入造成的影响随着 Agent 能力的增长而扩大
在上下文层面，解决提示注入问题的核心——<mark style="background:#d3f8b6">帮模型分清指令与数据</mark>——上下文层的防御只是第一道防御，后面还有执行层的防御
包括：
1. 来源标记：在外部内容注入上下文之前，用明确的标记包裹并标注来源，提示模型这段内容中出现的“指令”不应被执行
2. 结构化角色：利用 Chat Template 的角色体系，让模型区分可信指令与外部数据——以上两条都类似于XML标签
3. 输入清洗：过滤外部内容中的可疑内容；这层防御容易被措辞变体绕过，只能作为辅助手段

提示注入攻防实验——揭露有哪些提示注入的攻击方式
1. 直接注入：在用户消息中直接输入攻击指令
2. 间接注入：用户要求 Agent “总结这个网页的内容”，而网页正文中嵌入了不可见的攻击指令
3. 记忆注入：在多轮对话中，攻击者在某个会话中植入看似无害的上下文片段

#### 动态提示词
出现背景：Agent覆盖场景越多，系统提示词会不断膨胀，造成两个问题：token被浪费；注意力被稀释（模型能力下降）
Skill的渐进式披露细则：
1. 元数据：每个 Skill 必须包含一个 `SKILL.md` 文件，开头是 YAML frontmatter，包含 `name` 和 `description` 两个字段。Agent 框架在启动时扫描所有已安装的 Skill，将它们的 `name` 和 `description`注入到对话上下文中
2. 正文：当 Agent 判断某个任务需要特定的 Skill 时，通过专用的 Skill 工具加载完整的 `SKILL.md`
3. SKILL.md引用附件
重点：构建agent时，应对其使用模型厂商的方法论。例如，使用 Claude 构建 Agent 时，应充分利用 Skills 和结构化系统提示。这样可以更好地发挥模型的能力

Skill正文放在上下文的什么位置：
1. 注入系统提示词：模型对 system 位置的指令遵循能力最强（因为训练时大量使用了这个位置的指令），所以 Skill 的执行效果最好 。但是每次加载新的 Skill 都会改变 system 消息的内容，导致 KV Cache 前缀失效，延迟和成本显著增加
2. 内容出现在上下文中间：这种方式不影响KV Cache，但是对模型是否能从上下文中间位置准确识别并遵循 Skill 中的指令要求很高，实际效果可能不佳
3. 渐进式披露：Skill 的元数据保证 KV Cache 前缀有效；具体需要skill内容的时候进入上下文

#### Agent状态栏
定义：Agent 框架向模型同步各种动态状态的统一机制——帮助agent动态地感知自身的状态和任务的进展
为什么需要状态栏：Agent 缺乏对环境当前状态的感知和对任务进展的跟踪能力，执行复杂任务时容易陷入各种陷阱，例如：无限循环、状态遗忘、任务目标偏离。Agent 状态栏通过在上下文中嵌入结构化的元信息，为 Agent 提供自我感知和自我调节
	 Agent状态栏的理论基础——为什么Agent对状态的感知能力差？
	 注意力机制的本质特性：上下文学习更像检索而非推理——模型擅长从已有内容中查找信息，但不擅长主动归纳和总结。主动归纳的信息淹没在大量的原始记录中。模型每次确定状态都需要花费额外的思考 token 去扫描上下文重新统计，这个过程效率极低且错误率很高
特征：动态的。系统提示词为了保证 KV Cache 有效，必须是静态的；但是Agent状态栏必须随着任务的推进不断更新，所以是动态的
同时，随着上下文长度的增加，模型会过度关注最近的上下文内容，将关键的元信息以结构化的形式放置在上下文末尾时，这些信息在空间上更接近模型即将生成的新 token，因而能获得更高的注意力权重
对于Agent状态栏的实战运用：
1. 任务规划：上下文内容过多导致Agent容易过度关注当前任务，而忽略原始诉求和后续工作。通过引入 TODO 列表将任务分解为清晰的步骤，放在轨迹末尾不断提醒模型当前的进展和未来的目标，确保行动与总体规划保持一致——这一点也要注意，由于模型几乎完全相信状态栏，所以如果状态栏出现了错误，模型必然也会出错
2. 事件的侧信道信息：为每个事件附加元数据——精确的时间、地理位置、距上次 Agent 回复的时间间隔等。这些信息帮助模型理解事件的时序关系和环境背景，从而做出更符合情境的决策。
3. 环境的当前状态：动态的环境信息、异常操作提醒、以及从隐式状态到显式状态的转换等
4. 可用能力清单：已安装 Skill 的元数据列表也走这条同一的末尾注入通道

任务规划和环境的当前状态是变化的；事件的侧信道信息和可用能力清单是不变的（或者说变化是完全可预见的，部分的）
任务规划和环境状态这种动态信息是Agent框架以特殊的用户消息追加到上下文末尾，并随任务推进不断更新——这是为了避免之前提过的KV Cache的缓存问题
既然需要<mark style="background:#d3f8b6">更新状态</mark>，必然会存在缓存成本。有两条实现路径，分别有不同的缓存代价：
1. 每轮替换：每次 API 调用前，从消息列表中移除上一轮的状态消息，在末尾追加最新状态
	优点：状态信息有限，不会耗费过多的token
	缺点：每次移除之前的状态信息，导致移除部分之后的上下文的KV Cache缓存失效。但是失效范围有限，只有最后一部分内容
2. 持久追加：之前的状态消息不动，每轮在末尾追加新的状态消息
	优点：前缀始终保持稳定，不破坏缓存
	缺点：过去的状态信息在上下文中累积，占用token和上下文。过去的状态可能对模型的判断产生影响

如何取舍两种实现路径：
	方法一：轨迹较短或单条状态消息很大。末尾几轮的缓存失效本来就便宜，换来的是上下文的整洁和无歧义
	方法二：状态更新频繁且轨迹很长。每轮替换带来的缓存失效会在长轨迹上反复累积，代价远超陈旧状态占用的 token

注意，只给状态对模型并没有较大的改善。必须同时给出“这些读数该怎么用”的操作手册
即将抽象的状态量化成模型需要遵守的规则。此时，状态的意义才能被模型真正发挥

#### 上下文压缩策略（重中之重）
为什么要压缩上下文：
1. 上下文长度、成本、效率：上下文窗口有限；上下文越多，token消耗量越大、推理能力下降
2. ==提升思考质量==：
	1. <mark style="background:#d3f8b6">总结后的上下文比原来的上下文更利于模型使用</mark>——我现在想起来，我对上下文压缩，尤其是我设计后的上下文压缩的内容一窍不通（如果是以todo列表的形式和总结好的文档对上下文进行压缩，那么压缩其实是一件好事）——那如果这样看，在合适的时间压缩能够将上下文压缩从一件坏事转变为好事（ECC的上下文压缩时机要考虑和codex的上下文实际情况相结合，从而加入到develop流程中）
	2. <mark style="background:#d3f8b6">把需要思考才能得到的结论变成可以直接检索的知识</mark>
	3. 长上下文会导致检索精度的下降——上下文腐化：上下文窗口还远没有满，但 Agent 突然找不到关键信息了，或者反复纠结于一个早已解决的问题
	   ——注意力机制：上下文越长，无关的内容越多，注意力权重被分散到无关的token上，导致Agent的能力下降

压缩上下文的缺点：
1. 压缩上下文要额外调用API，造成额外的消耗
2. 压缩后，信息必然会失真
3. 压缩会导致缓存被破坏，因此必须控制压缩的频率

上下文压缩发生的时间与对象：
1. 时间：两次API调动之间
2. 对象：不是API实现的，而是模型框架实现的——需要调用额外的API（只有在进行上下文智能摘要的时候需要）

上下文压缩策略：
1.  非任务感知压缩：对所有历史消息进行压缩。为每一类消息都进行直接压缩，没有智能筛选。因为这些消息之间可能有重复，导致压缩的内容也是重复的；而且这些压缩的内容之间没有相关性
2. 上下文感知压缩：将当前的查询意图和已积累的信息纳入压缩的决策过程，即带脑子压缩。对过去和未来任务的判断后，对压缩的内容有了取舍
3. 带引用的上下文感知：在上下文感知压缩的基础上增加了引用，每条事实都附带来源的引用标记
4. 自适应窗口化：根据当前上下文使用情况，在接近容量限制时启动压缩机制

生产级的Agent对于不同的上下文应该采取不同的策略：
1. 工具调用结果：工具调用结果内容很多，通过压缩得到摘要。尤其是最终结果而不是工具调用过程和错误结果
2. 噪声：低价值的内容（噪声）直接移除，不做摘要
3. API层压缩：API层移除上下文中的工具调用结果（发给模型的内容），但是本地消息保持不变
4. 归档式摘要：对每一轮的上下文做结构化摘要
5. 全量压缩：由LLM驱动的完整压缩

具体压缩策略如何实现实现的几条原则：
1. 信息价值的非均匀分布：根据信息的价值进行有重点的压缩
2. 语义完整性：保证压缩后的内容语义是完整的，没有偏离
3. 任务相关性：根据任务要求，对压缩的内容和结果有取舍
4. 压缩即理解：有效的压缩应当是对语义真正的理解——压缩的结果应当是可审查的、可跨会话复用的
5. 压缩策略与任务类型匹配：信息检索类的任务需要保留广度，分析类的任务需要保留深度，创作类的任务需要保留灵感触发点
6. 压缩最容易丢失的不是细节本身，而是早期的架构决策、约束背后的理由和失败的路径
——==为什么压缩容易丢失早期架构决策、约束理由和失败路径等？我的理解是这些内容往往原本就是低频出现、抽象程度高、依赖因果关系才能理解的信息，而不是简单的事实。上下文压缩时很容易使这些抽象的内容失去原意==


