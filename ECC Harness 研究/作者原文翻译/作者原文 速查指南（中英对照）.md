# The Shorthand Guide to Everything Claude Code · 速查指南

> 作者：cogsec / @affaan (Affaan Mustafa)　|　原文：https://x.com/affaan/status/2012378465664745795
> 发布：2026-01-17　|　抓取整理：2026-08-03（第 2 版，按语义单元重排）
>
> **阅读方式**：每个小节先给作者的**英文原文**（保留他自己的段落划分），紧跟一整块 `> **中文**` 译文。
> 代码块保留原样，块内注释已译。图片说明文字以 *斜体* 标注（原推文里是截图，此处只留文字说明）。

---

Here's my complete setup after 10 months of daily use: skills, hooks, subagents, MCPs, plugins, and what actually works.

Been an avid Claude Code user since the experimental rollout in Feb, and won the Anthropic x Forum Ventures hackathon with Zenith alongside @DRodriguezFX completely using Claude Code.

> **中文**
>
> 这是我日常使用 10 个月后的完整配置：skills、hooks、subagents、MCP、插件，以及哪些是真正管用的。
>
> 自 2 月实验性推出以来我就是 Claude Code 的重度用户，还和 @DRodriguezFX 一起用 Claude Code 全程开发了 Zenith，拿下了 Anthropic × Forum Ventures 黑客松冠军。


## Skills and Commands · Skills 与 Commands

Skills operate like rules, constricted to certain scopes and workflows. They're shorthand to prompts when you need to execute a particular workflow.

After a long session of coding with Opus 4.5, you want to clean out dead code and loose .md files?

Run /refactor-clean. Need testing? /tdd, /e2e, /test-coverage. Skills and commands can be chained together in a single prompt

> **中文**
>
> Skill 的运作方式类似规则，被约束在特定的作用域和工作流里。当你需要执行某个特定工作流时，它就是提示词的速记形式。
>
> 用 Opus 4.5 写了很久的代码之后，想清理掉无用代码和散落的 .md 文件？
>
> 运行 /refactor-clean。需要测试？/tdd、/e2e、/test-coverage。Skill 和 command 可以在同一条提示词里串联使用。

*（图）chaining commands together*
*（图）把多个命令串起来用*

I can make a skill that updates codemaps at checkpoints - a way for Claude to quickly navigate your codebase without burning context on exploration.

> **中文**
>
> 我可以做一个在检查点更新 codemap（代码地图）的 skill——这样 Claude 能快速在你的代码库里导航，而不必把上下文烧在探索上。

```
~/.claude/skills/codemap-updater.md
```

Commands are skills executed via slash commands. They overlap but are stored differently:

> **中文**
>
> Command 就是通过斜杠命令执行的 skill。两者有重叠，但存放位置不同：

Skills: ~/.claude/skills - broader workflow definitions  
Commands: ~/.claude/commands - quick executable prompts

> **中文**
>
> Skills：~/.claude/skills —— 更宽泛的工作流定义
> Commands：~/.claude/commands —— 可快速执行的提示词

```bash
# Example skill structure
~/.claude/skills/
  pmx-guidelines.md      # Project-specific patterns
  coding-standards.md    # Language best practices
  tdd-workflow/          # Multi-file skill with README.md
  security-review/       # Checklist-based skill
```

> **中文**
>
> ```bash
> # skill 结构示例
> ~/.claude/skills/
>   pmx-guidelines.md      # 项目专属模式
>   coding-standards.md    # 语言最佳实践
>   tdd-workflow/          # 带 README.md 的多文件 skill
>   security-review/       # 基于检查清单的 skill
> ```


## Hooks · Hooks（钩子）

Hooks are trigger-based automations that fire on specific events. Unlike skills, they're constricted to tool calls and lifecycle events.

> **中文**
>
> Hook 是基于触发器的自动化，在特定事件发生时触发。和 skill 不同，它被约束在工具调用和生命周期事件上。


### Hook Types · Hook 类型

```
PreToolUse  - Before a tool executes (validation, reminders)
PostToolUse - After a tool finishes (formatting, feedback loops)
UserPromptSubmit - When you send a message
Stop - When Claude finishes responding
PreCompact - Before context compaction
Notification - Permission requests
```

> **中文**
>
> ```
> PreToolUse  —— 工具执行之前（做校验、提醒）
> PostToolUse —— 工具执行完成之后（格式化、反馈回路）
> UserPromptSubmit —— 你发送消息时
> Stop —— Claude 回复结束时
> PreCompact —— 上下文压缩之前
> Notification —— 权限请求时
> ```

Example: tmux reminder before long-running commands

> **中文**
>
> 示例：在运行耗时命令前提醒使用 tmux

```json
{
  "PreToolUse": [
    {
      "matcher": "tool == \"Bash\" && tool_input.command matches \"(npm|pnpm|yarn|cargo|pytest)\"",
      "hooks": [
        {
          "type": "command",
          "command": "if [ -z \"$TMUX\" ]; then echo '[Hook] Consider tmux for session persistence' >&2; fi"
        }
      ]
    }
  ]
}
Example of what feedback you get in Claude Code, while running a PostToolUse hook
```

> **中文**
>
> ```json
> {
>   "PreToolUse": [
>     {
>       "matcher": "tool == \"Bash\" && tool_input.command matches \"(npm|pnpm|yarn|cargo|pytest)\"",
>       "hooks": [
>         {
>           "type": "command",
>           "command": "if [ -z \"$TMUX\" ]; then echo '[Hook] Consider tmux for session persistence' >&2; fi"
>         }
>       ]
>     }
>   ]
> }
> （上面这段的含义：当 Bash 命令里包含 npm/pnpm/yarn/cargo/pytest 时，如果当前不在 tmux 会话中，就往 stderr 打一条"建议用 tmux 保持会话"的提醒。）
> 运行 PostToolUse hook 时，你在 Claude Code 里会收到的反馈示例
> ```

Pro tip: Use the `hookify` plugin to create hooks conversationally instead of writing JSON manually. Run /hookify and describe what you want.

> **中文**
>
> 小技巧：用 `hookify` 插件以对话方式创建 hook，不用手写 JSON。运行 /hookify，然后描述你想要什么即可。


## Subagents · Subagents（子智能体）

Subagents are processes your orchestrator (main Claude) can delegate tasks to with limited scopes. They can run in background or foreground, freeing up context for the main agent.

Subagents work nicely with skills - a subagent capable of executing a subset of your skills can be delegated tasks and use those skills autonomously. They can also be sandboxed with specific tool permissions.

> **中文**
>
> Subagent 是你的编排者（主 Claude）可以委派任务的进程，作用域受限。它们可以在后台或前台运行，从而为主 agent 腾出上下文。
>
> Subagent 和 skill 配合得很好——一个有能力执行你部分 skill 的 subagent，可以被委派任务并自主使用这些 skill。它们还可以用特定的工具权限做沙箱隔离。

```bash
# Example subagent structure
~/.claude/agents/
  planner.md           # Feature implementation planning
  architect.md         # System design decisions
  tdd-guide.md         # Test-driven development
  code-reviewer.md     # Quality/security review
  security-reviewer.md # Vulnerability analysis
  build-error-resolver.md
  e2e-runner.md
  refactor-cleaner.md
```

> **中文**
>
> ```bash
> # subagent 结构示例
> ~/.claude/agents/
>   planner.md           # 功能实现规划
>   architect.md         # 系统设计决策
>   tdd-guide.md         # 测试驱动开发
>   code-reviewer.md     # 质量／安全审查
>   security-reviewer.md # 漏洞分析
>   build-error-resolver.md
>   e2e-runner.md
>   refactor-cleaner.md
> ```

Configure allowed tools, MCPs, and permissions per subagent for proper scoping.

> **中文**
>
> 为每个 subagent 单独配置允许的工具、MCP 和权限，做好作用域划分。


## Rules and Memory · Rules 与 Memory（规则与记忆）

Your `.rules` folder holds `.md` files with best practices Claude should ALWAYS follow. Two approaches:

> **中文**
>
> 你的 `.rules` 文件夹里放的是 `.md` 文件，写的是 Claude **必须始终**遵守的最佳实践。有两种做法：

Single CLAUDE.md - Everything in one file (user or project level)  
Rules folder - Modular `.md` files grouped by concern

> **中文**
>
> 单个 CLAUDE.md —— 所有内容放一个文件里（用户级或项目级）
> Rules 文件夹 —— 按关注点分组的模块化 `.md` 文件

```bash
~/.claude/rules/
  security.md      # No hardcoded secrets, validate inputs
  coding-style.md  # Immutability, file organization
  testing.md       # TDD workflow, 80% coverage
  git-workflow.md  # Commit format, PR process
  agents.md        # When to delegate to subagents
  performance.md   # Model selection, context management
```

> **中文**
>
> ```bash
> ~/.claude/rules/
>   security.md      # 不硬编码密钥、校验输入
>   coding-style.md  # 不可变性、文件组织
>   testing.md       # TDD 工作流、80% 覆盖率
>   git-workflow.md  # 提交格式、PR 流程
>   agents.md        # 何时委派给 subagent
>   performance.md   # 模型选择、上下文管理
> ```

Example rules:

> **中文**
>
> 规则示例：

No emojis in codebase  
Refrain from purple hues in frontend  
Always test code before deployment  
Prioritize modular code over mega-files  
Never commit console.logs

> **中文**
>
> 代码库里不准出现 emoji
> 前端避免使用紫色调
> 部署前必须测试代码
> 优先写模块化代码，而不是巨型文件
> 永远不要提交 console.log


## MCPs (Model Context Protocol) · MCP（模型上下文协议）

MCPs connect Claude to external services directly. Not a replacement for APIs - it's a prompt-driven wrapper around them, allowing more flexibility in navigating information.

Example: Supabase MCP lets Claude pull specific data, run SQL directly upstream without copy-paste. Same for databases, deployment platforms, etc.

> **中文**
>
> MCP 把 Claude 直接连到外部服务。它不是 API 的替代品——它是包在 API 外面的、提示词驱动的一层封装，让信息导航更灵活。
>
> 举例：Supabase MCP 让 Claude 直接拉取特定数据、在上游直接跑 SQL，不用复制粘贴。数据库、部署平台等同理。

*（图）Example of the supabase mcp listing the tables within the public schema*
*（图）Supabase MCP 列出 public schema 下所有数据表的示例*

Chrome in Claude: is a built-in plugin MCP that lets Claude autonomously control your browser - clicking around to see how things work.

> **中文**
>
> Chrome in Claude：这是一个内置的插件式 MCP，让 Claude 自主控制你的浏览器——点点点地去看东西是怎么运作的。


### CRITICAL: Context Window Management · 关键：上下文窗口管理

Be picky with MCPs. I keep all MCPs in user config but disable everything unused. Navigate to /plugins and scroll down or run /mcp.

Your 200k context window before compacting might only be 70k with too many tools enabled. Performance degrades significantly.

> **中文**
>
> 挑 MCP 要挑剔。我把所有 MCP 都留在用户配置里，但把不用的全部禁用。去 /plugins 往下翻，或者运行 /mcp。
>
> 如果开了太多工具，你那个压缩前 200k 的上下文窗口可能只剩 70k。性能会显著下降。

*（图）using /plugins to navigate to MCPs to see which ones are currently installed and their status*
*（图）用 /plugins 进到 MCP 页面，查看当前装了哪些以及它们的状态*

Rule of thumb: Have 20-30 MCPs in config, but keep under 10 enabled / under 80 tools active.

> **中文**
>
> 经验法则：配置里放 20-30 个 MCP，但保持启用数少于 10 个、活跃工具数少于 80 个。


## Plugins · 插件

Plugins package tools for easy installation instead of tedious manual setup. A plugin can be a skill + MCP combined, or hooks/tools bundled together.

Installing plugins:

> **中文**
>
> 插件把工具打包好方便安装，省掉繁琐的手动配置。一个插件可以是 skill + MCP 的组合，也可以是 hook／工具的捆绑包。
>
> 安装插件：

```bash
# Add a marketplace
claude plugin marketplace add https://github.com/mixedbread-ai/mgrep
```

> **中文**
>
> ```bash
> # 添加一个应用市场
> claude plugin marketplace add https://github.com/mixedbread-ai/mgrep
> ```

```
# Open Claude, run /plugins, find new marketplace, install from there
displaying the newly installed Mixedbread-Grep marketplace
```

> **中文**
>
> ```
> # 打开 Claude，运行 /plugins，找到新加的市场，从那里安装
> 显示刚安装好的 Mixedbread-Grep 市场
> ```

LSP Plugins: are particularly useful if you run Claude Code outside editors frequently. Language Server Protocol gives Claude real-time type checking, go-to-definition, and intelligent completions without needing an IDE open.

> **中文**
>
> LSP 插件：如果你经常在编辑器之外运行 Claude Code，这类插件特别有用。语言服务器协议（LSP）给 Claude 提供实时类型检查、跳转到定义、智能补全，不需要开着 IDE。

```bash
# Enabled plugins example
typescript-lsp@claude-plugins-official  # TypeScript intelligence
pyright-lsp@claude-plugins-official     # Python type checking
hookify@claude-plugins-official         # Create hooks conversationally
mgrep@Mixedbread-Grep                   # Better search than ripgrep
```

> **中文**
>
> ```bash
> # 已启用插件示例
> typescript-lsp@claude-plugins-official  # TypeScript 智能提示
> pyright-lsp@claude-plugins-official     # Python 类型检查
> hookify@claude-plugins-official         # 以对话方式创建 hook
> mgrep@Mixedbread-Grep                   # 比 ripgrep 更好的搜索
> ```

Same warning as MCPs - watch your context window.

> **中文**
>
> 和 MCP 一样的警告——盯住你的上下文窗口。


## Tips and Tricks · 技巧与窍门


### Keyboard Shortcuts · 键盘快捷键

```
Ctrl+U - Delete entire line (faster than backspace spam)
! - Quick bash command prefix
@ - Search for files
/ - Initiate slash commands
Shift+Enter - Multi-line input
Tab - Toggle thinking display
Esc Esc - Interrupt Claude / restore code
```

> **中文**
>
> ```
> Ctrl+U —— 删除整行（比狂按退格快）
> ! —— 快速执行 bash 命令的前缀
> @ —— 搜索文件
> / —— 唤起斜杠命令
> Shift+Enter —— 多行输入
> Tab —— 切换思考过程显示
> Esc Esc —— 打断 Claude／恢复代码
> ```


### Parallel Workflows · 并行工作流

/fork - Fork conversations to do non-overlapping tasks in parallel instead of spamming queued messages

Git Worktrees - For overlapping parallel Claudes without conflicts. Each worktree is an independent checkout

> **中文**
>
> /fork —— 分叉对话，并行处理互不重叠的任务，而不是一股脑排队发消息
>
> Git Worktrees —— 用于让多个 Claude 并行工作而互不冲突。每个 worktree 都是一份独立的检出。

```bash
git worktree add ../feature-branch feature-branch
# Now run separate Claude instances in each worktree
```

> **中文**
>
> ```bash
> git worktree add ../feature-branch feature-branch
> # 现在可以在每个 worktree 里跑各自独立的 Claude 实例
> ```

tmux for Long-Running Commands: Stream and watch logs/bash processes Claude runs.

> **中文**
>
> 用 tmux 跑耗时命令：可以流式查看 Claude 运行的日志／bash 进程。

*（图）letting claude code spin up the frontend and backend servers and monitoring the logs by attaching to the session using tmux*
*（图）让 Claude Code 把前后端服务器都起起来，然后通过 attach 到 tmux 会话来监控日志*

```bash
tmux new -s dev
# Claude runs commands here, you can detach and reattach
tmux attach -t dev
```

> **中文**
>
> ```bash
> tmux new -s dev
> # Claude 在这里跑命令，你可以随时 detach 再 attach 回来
> tmux attach -t dev
> ```

mgrep > grep: `mgrep` is a significant improvement from ripgrep/grep. Install via plugin marketplace, then use the /mgrep skill. Works with both local search and web search.

> **中文**
>
> mgrep > grep：`mgrep` 相比 ripgrep/grep 是一次显著升级。通过插件市场安装，然后使用 /mgrep skill。本地搜索和网页搜索都支持。

```bash
mgrep "function handleSubmit"  # Local search
mgrep --web "Next.js 15 app router changes"  # Web search
```

> **中文**
>
> ```bash
> mgrep "function handleSubmit"  # 本地搜索
> mgrep --web "Next.js 15 app router changes"  # 网页搜索
> ```


### Other Useful Commands · 其它有用的命令

```
/rewind - Go back to a previous state
/statusline - Customize with branch, context %, todos
/checkpoints - File-level undo points
/compact - Manually trigger context compaction
```

> **中文**
>
> ```
> /rewind —— 回退到之前的状态
> /statusline —— 自定义状态栏，显示分支、上下文百分比、待办
> /checkpoints —— 文件级的撤销点
> /compact —— 手动触发上下文压缩
> ```


## GitHub Actions CI/CD · GitHub Actions CI/CD

Set up code review on your PRs with GitHub Actions. Claude can review PRs automatically when configured.

> **中文**
>
> 用 GitHub Actions 给你的 PR 配上代码审查。配置好之后 Claude 能自动审查 PR。

*（图）claude approving a bug fix PR*
*（图）Claude 批准一个 bug 修复 PR*


## Sandboxing · 沙箱隔离

Use sandbox mode for risky operations - Claude runs in restricted environment without affecting your actual system. (Use --dangerously-skip-permissions - to do the opposite of this and let claude roam free, this can be destructive if not careful.)

> **中文**
>
> 高风险操作用沙箱模式——Claude 在受限环境里运行，不会影响你的真实系统。（`--dangerously-skip-permissions` 是反过来的：让 Claude 完全放飞，不小心的话会有破坏性后果。）


## On Editors · 关于编辑器

While an editor isn't needed it can positively or negatively impact your Claude Code workflow. While Claude Code works from any terminal, pairing it with a capable editor unlocks real-time file tracking, quick navigation, and integrated command execution.

> **中文**
>
> 虽然编辑器不是必需品，但它会正面或负面地影响你的 Claude Code 工作流。Claude Code 在任何终端里都能跑，但配上一个称手的编辑器可以解锁实时文件追踪、快速导航和集成的命令执行。


### Zed (My Preference) · Zed（我的选择）

I use Zed - a Rust-based editor that's lightweight, fast, and highly customizable.

Why Zed works well with Claude Code:

> **中文**
>
> 我用 Zed —— 一个基于 Rust 的编辑器，轻量、快速、高度可定制。
>
> Zed 和 Claude Code 配合得好的原因：

Agent Panel Integration - Zed's Claude integration lets you track file changes in real-time as Claude edits. Jump between files Claude references without leaving the editor  
Performance - Written in Rust, opens instantly and handles large codebases without lag  
CMD+Shift+R Command Palette - Quick access to all your custom slash commands, debuggers, and tools in a searchable UI. Even if you just want to run a quick command without switching to terminal  
Minimal Resource Usage - Won't compete with Claude for system resources during heavy operations  
Vim Mode - Full vim keybindings if that's your thing  
Zed Editor with custom commands dropdown using CMD+Shift+R. Following mode shown as the bullseye in the bottom right.  
Split your screen - Terminal with Claude Code on one side, editor on the other using  
Ctrl + G  - quickly open the file Claude is currently working on in Zed  
Auto-save - Enable autosave so Claude's file reads are always current  
Git integration - Use editor's git features to review Claude's changes before committing  
File watchers - Most editors auto-reload changed files, verify this is enabled

> **中文**
>
> Agent 面板集成 —— Zed 的 Claude 集成让你在 Claude 编辑时实时追踪文件变更。不离开编辑器就能在 Claude 引用的文件之间跳转
> 性能 —— Rust 写的，秒开，处理大型代码库不卡
> CMD+Shift+R 命令面板 —— 在一个可搜索的界面里快速访问你所有的自定义斜杠命令、调试器和工具。哪怕只是想跑个小命令也不用切到终端
> 资源占用极小 —— 重负载操作时不会和 Claude 抢系统资源
> Vim 模式 —— 如果你吃这套，它有完整的 vim 键位
> Zed 编辑器，用 CMD+Shift+R 唤出自定义命令下拉框。右下角那个靶心图标是 following 模式。
> 分屏 —— 一边是跑着 Claude Code 的终端，另一边是编辑器
> Ctrl + G —— 在 Zed 里快速打开 Claude 当前正在处理的文件
> 自动保存 —— 开启自动保存，这样 Claude 读到的文件永远是最新的
> Git 集成 —— 用编辑器的 git 功能在提交前审查 Claude 的改动
> 文件监听 —— 大多数编辑器会自动重载变更的文件，确认这个功能是开着的


### VSCode / Cursor · VSCode / Cursor

This is also a viable choice and works well with Claude Code. You can use it in either terminal format, with automatic sync with your editor using \ide enabling LSP functionality (somewhat redundant with plugins now). Or you can opt for the extension which is more integrated with the Editor and has a matching UI.

> **中文**
>
> 这也是个可行的选择，和 Claude Code 配合良好。你可以用终端形式，通过 `\ide` 与编辑器自动同步来启用 LSP 功能（现在有了插件，这一点略显冗余）。或者你也可以选扩展版，它和编辑器集成得更深，UI 也更统一。

*（图）from the docs directly at https://code.claude.com/docs/en/vs-code*
*（图）直接摘自官方文档 https://code.claude.com/docs/en/vs-code*


## My Setup · 我的配置


### Plugins · 插件

Installed: (I usually only have 4-5 of these enabled at a time)

> **中文**
>
> 已安装的：（我通常同一时间只启用其中 4-5 个）

```markdown
ralph-wiggum@claude-code-plugins       # Loop automation
frontend-design@claude-code-plugins    # UI/UX patterns
commit-commands@claude-code-plugins    # Git workflow
security-guidance@claude-code-plugins  # Security checks
pr-review-toolkit@claude-code-plugins  # PR automation
typescript-lsp@claude-plugins-official # TS intelligence
hookify@claude-plugins-official        # Hook creation
code-simplifier@claude-plugins-official
feature-dev@claude-code-plugins
explanatory-output-style@claude-code-plugins
code-review@claude-code-plugins
context7@claude-plugins-official       # Live documentation
pyright-lsp@claude-plugins-official    # Python types
mgrep@Mixedbread-Grep                  # Better search
```

> **中文**
>
> ```markdown
> ralph-wiggum@claude-code-plugins       # 循环自动化
> frontend-design@claude-code-plugins    # UI/UX 模式
> commit-commands@claude-code-plugins    # Git 工作流
> security-guidance@claude-code-plugins  # 安全检查
> pr-review-toolkit@claude-code-plugins  # PR 自动化
> typescript-lsp@claude-plugins-official # TS 智能提示
> hookify@claude-plugins-official        # 创建 hook
> code-simplifier@claude-plugins-official
> feature-dev@claude-code-plugins
> explanatory-output-style@claude-code-plugins
> code-review@claude-code-plugins
> context7@claude-plugins-official       # 实时文档
> pyright-lsp@claude-plugins-official    # Python 类型
> mgrep@Mixedbread-Grep                  # 更好的搜索
> ```


### MCP Servers · MCP 服务器

Configured (User Level):

> **中文**
>
> 已配置（用户级）：

```json
{
  "github": { "command": "npx", "args": ["-y", "@modelcontextprotocol/server-github"] },
  "firecrawl": { "command": "npx", "args": ["-y", "firecrawl-mcp"] },
  "supabase": {
    "command": "npx",
    "args": ["-y", "@supabase/mcp-server-supabase@latest", "--project-ref=YOUR_REF"]
  },
  "memory": { "command": "npx", "args": ["-y", "@modelcontextprotocol/server-memory"] },
  "sequential-thinking": {
    "command": "npx",
    "args": ["-y", "@modelcontextprotocol/server-sequential-thinking"]
  },
  "vercel": { "type": "http", "url": "https://mcp.vercel.com" },
  "railway": { "command": "npx", "args": ["-y", "@railway/mcp-server"] },
  "cloudflare-docs": { "type": "http", "url": "https://docs.mcp.cloudflare.com/mcp" },
  "cloudflare-workers-bindings": {
    "type": "http",
    "url": "https://bindings.mcp.cloudflare.com/mcp"
  },
  "cloudflare-workers-builds": { "type": "http", "url": "https://builds.mcp.cloudflare.com/mcp" },
  "cloudflare-observability": {
    "type": "http",
    "url": "https://observability.mcp.cloudflare.com/mcp"
  },
  "clickhouse": { "type": "http", "url": "https://mcp.clickhouse.cloud/mcp" },
  "AbletonMCP": { "command": "uvx", "args": ["ableton-mcp"] },
  "magic": { "command": "npx", "args": ["-y", "@magicuidesign/mcp@latest"] }
}
```

Disabled per project (context window management):

> **中文**
>
> 按项目禁用（上下文窗口管理）：

```markdown
# In ~/.claude.json under projects.[path].disabledMcpServers
disabledMcpServers: [
  "playwright",
  "cloudflare-workers-builds",
  "cloudflare-workers-bindings",
  "cloudflare-observability",
  "cloudflare-docs",
  "clickhouse",
  "AbletonMCP",
  "context7",
  "magic"
]
```

> **中文**
>
> ```markdown
> # 位于 ~/.claude.json 的 projects.[路径].disabledMcpServers 下
> disabledMcpServers: [
>   "playwright",
>   "cloudflare-workers-builds",
>   "cloudflare-workers-bindings",
>   "cloudflare-observability",
>   "cloudflare-docs",
>   "clickhouse",
>   "AbletonMCP",
>   "context7",
>   "magic"
> ]
> ```

This is the key - I have 14 MCPs configured but only ~ 5-6 enabled per project. Keeps context window healthy.

> **中文**
>
> 这是关键——我配置了 14 个 MCP，但每个项目只启用大约 5-6 个。这样上下文窗口才健康。


### Key Hooks · 关键 hook

```json
{
  "PreToolUse": [
    // tmux reminder for long-running commands
    { "matcher": "npm|pnpm|yarn|cargo|pytest", "hooks": ["tmux reminder"] },
    // Block unnecessary .md file creation
    { "matcher": "Write && .md file", "hooks": ["block unless README/CLAUDE"] },
    // Review before git push
    { "matcher": "git push", "hooks": ["open editor for review"] }
  ],
  "PostToolUse": [
    // Auto-format JS/TS with Prettier
    { "matcher": "Edit && .ts/.tsx/.js/.jsx", "hooks": ["prettier --write"] },
    // TypeScript check after edits
    { "matcher": "Edit && .ts/.tsx", "hooks": ["tsc --noEmit"] },
    // Warn about console.log
    { "matcher": "Edit", "hooks": ["grep console.log warning"] }
  ],
  "Stop": [
    // Audit for console.logs before session ends
    { "matcher": "*", "hooks": ["check modified files for console.log"] }
  ]
}
```

> **中文**
>
> ```json
> {
>   "PreToolUse": [
>     // 耗时命令的 tmux 提醒
>     { "matcher": "npm|pnpm|yarn|cargo|pytest", "hooks": ["tmux reminder"] },
>     // 阻止创建不必要的 .md 文件
>     { "matcher": "Write && .md file", "hooks": ["block unless README/CLAUDE"] },
>     // git push 前先审查
>     { "matcher": "git push", "hooks": ["open editor for review"] }
>   ],
>   "PostToolUse": [
>     // 用 Prettier 自动格式化 JS/TS
>     { "matcher": "Edit && .ts/.tsx/.js/.jsx", "hooks": ["prettier --write"] },
>     // 编辑后做 TypeScript 检查
>     { "matcher": "Edit && .ts/.tsx", "hooks": ["tsc --noEmit"] },
>     // 对 console.log 发出警告
>     { "matcher": "Edit", "hooks": ["grep console.log warning"] }
>   ],
>   "Stop": [
>     // 会话结束前审计有没有残留 console.log
>     { "matcher": "*", "hooks": ["check modified files for console.log"] }
>   ]
> }
> ```


### Custom Status Line · 自定义状态栏

Shows user, directory, git branch with dirty indicator, context remaining %, model, time, and todo count:

> **中文**
>
> 显示用户、目录、带脏标记的 git 分支、剩余上下文百分比、模型、时间和待办数量：

*（图）example statusline in my Mac root directory*
*（图）我 Mac 根目录下的状态栏示例*


### Rules Structure · Rules 目录结构

```markdown
~/.claude/rules/
  security.md      # Mandatory security checks
  coding-style.md  # Immutability, file size limits
  testing.md       # TDD, 80% coverage
  git-workflow.md  # Conventional commits
  agents.md        # Subagent delegation rules
  patterns.md      # API response formats
  performance.md   # Model selection (Haiku vs Sonnet vs Opus)
  hooks.md         # Hook documentation
```

> **中文**
>
> ```markdown
> ~/.claude/rules/
>   security.md      # 强制性安全检查
>   coding-style.md  # 不可变性、文件大小限制
>   testing.md       # TDD、80% 覆盖率
>   git-workflow.md  # 约定式提交
>   agents.md        # subagent 委派规则
>   patterns.md      # API 响应格式
>   performance.md   # 模型选择（Haiku vs Sonnet vs Opus）
>   hooks.md         # hook 文档
> ```


### Subagents · Subagents

```markdown
~/.claude/agents/
  planner.md           # Break down features
  architect.md         # System design
  tdd-guide.md         # Write tests first
  code-reviewer.md     # Quality review
  security-reviewer.md # Vulnerability scan
  build-error-resolver.md
  e2e-runner.md        # Playwright tests
  refactor-cleaner.md  # Dead code removal
  doc-updater.md       # Keep docs synced
```

> **中文**
>
> ```markdown
> ~/.claude/agents/
>   planner.md           # 拆解功能
>   architect.md         # 系统设计
>   tdd-guide.md         # 先写测试
>   code-reviewer.md     # 质量审查
>   security-reviewer.md # 漏洞扫描
>   build-error-resolver.md
>   e2e-runner.md        # Playwright 测试
>   refactor-cleaner.md  # 无用代码清理
>   doc-updater.md       # 保持文档同步
> ```


## Key Takeaways · 核心要点

Don't overcomplicate - treat configuration like fine-tuning, not architecture  
Context window is precious - disable unused MCPs and plugins  
Parallel execution - fork conversations, use git worktrees  
Automate the repetitive - hooks for formatting, linting, reminders  
Scope your subagents - limited tools = focused execution

> **中文**
>
> 别搞复杂了 —— 把配置当成微调来对待，而不是当成架构
> 上下文窗口很宝贵 —— 禁用不用的 MCP 和插件
> 并行执行 —— fork 对话、用 git worktree
> 把重复的事自动化 —— 用 hook 做格式化、lint、提醒
> 给 subagent 划好作用域 —— 工具受限 = 执行更聚焦


## References · 参考资料

- Plugins Reference
- Hooks Documentation
- Checkpointing
- Interactive Mode
- Memory System
- [Subagents]
- [MCP Overview]

> **中文**
>
> - 插件参考文档
> - Hooks 文档
> - 检查点（Checkpointing）
> - 交互模式
> - 记忆系统
> - [Subagents]
> - [MCP 概览]

---

Note: This is a subset of detail. I might make more posts on specifics if people are interested.

4:16 · 2026年1月17日  
Views

> **中文**
>
> 注：这只是一部分细节。如果大家感兴趣，我可能会针对具体内容再写几篇。
>
> 2026 年 1 月 17 日 4:16
> 浏览量
