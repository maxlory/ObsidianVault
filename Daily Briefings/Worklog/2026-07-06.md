# 2026-07-06 工作日报

## AI 产品技能全景 SKILL 库（核心工作）

- **SKILL 1（需求拆解）构建 & 评测完成**：按 skill-builder 流程完成 3 道题（金融/客服/模糊题）的 with/without 对照评测，with-skill 13/13 (100%) vs 干净基线 36%，delta +0.64，技能增量真实有效
- **SKILL 2（Prompt 工程）回归评测**：复用原题原断言对重构后的 skill 跑回归，命中 eval_metadata 泄露卫生事故 → 作废泄露 run、干净重跑，结果待收尾
- **SKILL 3（评估设计）构建并提交审批**：设计完整输出 schema（整份 eval_plan 含 per-task 节）、TDD 三机制（门禁/冻结/回填）、校验脚本、3 references；SKILL.md 已过 plannotator 审阅
- **shared.schema.json 重构**：从 Bug Hunter 项目借鉴 `shared.schema.json` + `$ref` 模式，抽取 9 个共用枚举 + SubTask + IODEC 子结构，两技能 schema 合计减掉 300+ 行重复定义，校验脚本统一从共享文件读取枚举

## zap 会话管理优化（调研 & 方案）

- **调研 zap（Warp fork）的会话管理现状**：确定左侧面板已有 `ConversationListView` 但列的是内置 AI 会话，不是 Claude Code 会话；后台 `claude.rs` 插件已在实时追踪 Claude 会话数据
- **方案设计**：把「已记录的 Claude 会话」接入「已有会话列表面板」，点击触发 `claude --resume`。发现编译需完整 Xcode（~15GB），暂时卡在编译探路阶段

## AI 投研产品编排设计

- **评估 Agent-Smith 作为管理平台**：比对 9 节点投研计划后发现架构不匹配（Agent-Smith 的 "DAG" 是线性链、State 模型扁平、门禁聚焦软件开发领域），不适合做平台；但提炼出 4 个可复用设计模式（双层门禁、content hash 防重复、LLMGate 装饰器、重试渐进策略）
- **评估 Eino（Go 框架）**：编排能力与 LangGraph 对等甚至更强（Workflow 字段级映射），但纯 Go 生态导致金融数据工具链断裂，不推荐，建议继续用 LangGraph (Python)
- **完成 LangGraph 编排设计**：输出完整方案，含 13 节点精确契约、Evidence Quality Gate（4 项检查+FailureLoopGuard）、Bull/Bear 并行 Send() 扇出、QA 三级链统一回退、3 个人工介入点、逐节点 Token 预算控制

## 杂项

- **Claude 终端管理工具调研**：搜索带侧边栏会话列表的 Claude 终端 UI，发现 claude-squad（8k star TUI）、Seashell（Tauri 桌面应用）等候选，最终确定用户已在用 zap 且更倾向于在其上改造
- **PRD skill 搜索**：查询 prd 和 prd-development 两个 skill 是否支持 schema 输出，确认均为 Markdown 输出