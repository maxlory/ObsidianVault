# 2026-07-28 工作日报

## Agent 评测平台对比与 Claude Code 健康诊断

- 对比自研 Agent 测试平台（全量跑 vs 多 trial）与 Langfuse 评测报告，发现两平台题库不同、评分体系不同（二分制 vs 连续分数），自研内部 0 分歧；生成 HTML 对标报告。
- 运行 `/doctor` 健康诊断，检出 npm 重复安装、8 个零使用 inline 插件、无用户级权限默认值；提出清理建议但未执行。

## 量化假设卡 skill 开发与应用

- 完成 W1 收口：`quant-hypothesis-card` skill 已安装（150 行，零附件），对照评测 delta +0.42（盲评 + 污染检查通过）；门禁因 eval 过期未盖章，仅修了 validator 路径 bug，不绕 provenance 检查。
- 基于涨幅 >7% 套利教程，用该 skill 将「找涨幅 >7% 套利机会」的意图立成六要素假设卡，发现星网锐捷 / 锐捷网络两张图的对照价值（封板 vs 未封板）；卡在三处待拍板（样本区间、封板处理、证据不足阈值）。

## 微信实习信息提取系统调研与改造

- 调研 674 篇公众号文章的四种形态（单岗 / 多岗汇总 / 标题即全部 / 混合超长帖），量化 HTML 噪声率、433 种字段标签发散度、正则假阳性率；结论：脚本只负责清洗去噪，字段提取必须用 LLM。
- 将项目提取主力从规则改为 LLM（无 API key，纯 agent 底座），修复静默漏检（高盛工银理财等 5 个真漏检岗位已恢复），同时支持 agent 通道防幻觉校验；离线 + 真模型验收通过。

## Boss 直聘爬虫工作流文档化

- 将之前实现的 Boss 直聘 AI+金融实习岗爬虫完整工作流（Chrome CDP 设置 → 批量搜索 → 去重 → HTML 报表）整理为 AGENTS.md，Codex 直接可消费。

## 杂项

- 尝试使用 cc-agents 工具包将已有需求文档转为标准 PRD，因工具缺少图片/mermaid 组件且原始素材不足，未产出最终文档。