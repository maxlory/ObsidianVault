# 2026-05-26 工作日志

## ai-tasks TCC权限修复 & 自动保存
- 诊断macOS系统Python因SIP保护导致TCC权限不生效，webapp读不到`~/Documents`下已有plan文件，用户看到“任务消失”和“勾选状态丢失”
- 改用Homebrew Python 3.14 + venv路径指向项目`.venv/bin/python`，绕过SIP限制，webapp/checkin/review三个launchd服务均能正常读取plan.md
- 实现自动保存：checkbox勾选、文字编辑、意图修改均触发fetch POST（700ms debounce），关闭浏览器时sendBeacon兜底

## 财务分析skills测试设计
- 为已安装的3个skills设计对比测试方案：从v4历史题集中精选4道痛点题，覆盖multi-source检测、shortcut识别、sub-ledger协同能力
- 用户指出不应上传skill文件以免污染测试公平性，修正方案：只上传数据文件，观察agent能否通过prompt关键词自然触发已安装的skill

## ai-worklog reader.py改造
- 按用户3个维度选择（per-message时间戳过滤、跳过subagents目录、消息数<3跳过）完成reader.py改造
- 采用superpowers范式：writing-plans → execute + verification → commit，共耗时约10分钟
- 验证结果：跨天长session从229条消息精确削减至8条（仅当天部分），日报内容消除了历史污染