# 2026-06-02 工作日报

## AgentDaniel skill 路由调试与优化

- 分析 AgentDaniel trace（LangSmith 导出），定位“不调用 skill”根因：系统提示词广告了27个技能，但无一真正“拥有”从JE编报表的任务，且skill description含有大量“坚决不要触发”自逃逸条款
- 确定模型绕过 skill 的两层机制：octok 的 `skill_load` 只将 skill 正文作为参考注入上下文，模型可自由决策是否使用；同时模型有“自己能干”的强先验，会引用 description 中的跳过条件来合法绕过
- 删除过时记忆 `feedback_openclaw_skill_selection`（将“系统能做就别做 skill”错误泛化为“模型能干就不用 skill”）
- 优化 `name-description-authoring` skill：新增“负向边界只用于兄弟skill分流，绝不描述模型自己能干的条件”等4处改动，已落地

## AGENTS 配置优化与 skill 生态建设

- 创建新 skill `financial-analysis-report`（27指标口径库 + 9板块报告模板），完成4个核心文件（SKILL.md + 3个references），共441行，通过格式验证
- 将 AGENTS.md 改为全英文（v4.1→v4.2），同步 PANEL.md 改为英文版，融入利润表双轨（美式/中式）、口径统一等10处对齐修改
- 排查27个skill的功能重叠：确认2对真重复（decision-analysis→decision-analyzer、data-cleanup→clean-data-xls），移除3个 Antalpha 死配置 skill，更新 AGENTS（v4.4）和 PANEL
- 响应“skill绕过仍存在”，在 AGENTS 和 MEMORY 增加强制路由 protocol（v4.5→v4.6），但指出纯 prompt 约束边际收益递减，需依赖平台层物理锁
- 踢出能力不佳的 `report-generation`，将 `financial-analysis-report` 置入新文件处理链路末端（v4.7）；生成 v3.1→v4.4 演进对比HTML