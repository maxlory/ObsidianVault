
## Chapter 1: 配置系统与环境变量

#### 配置系统：
1. 例子：更高模型供应商、更改具体模型、更改默认语言等
	配置系统<font color="#92d050">像一个智能遥控器，让你不需要修改任何代码，就能轻松切换这些设置</font>
	引出==环境变量==：在启动系统时快速指定关键参数
2. 含义：配置是什么——配置系统有三个层级
	1. **默认配置**：系统自带的“安全”设置，即使你什么都不做，也能正常运行
	2. **配置文件**：你可以在项目根目录下放一个 **`.env` 文件**，写上你的个性化设置（长久实现）
	3. **环境变量**：<mark style="background:#d3f8b6">在启动命令前临时设置，优先级最高，用来覆盖其他所有设置</mark>（暂时性实现）

配置文件与环境变量的例子：选择AI模型
目的：修改系统使用的模型
方法：
1. **修改 .env 文件**：在你的项目文件夹里，创建一个名为 `.env` 的文件，并写入模型配置
2. **使用环境变量**：在终端运行命令时，直接指定模型
特点：不需要打开任何代码文件，就能实现配置系统——配置系统的意义
总结：==优先级规则——**命令行环境变量 > .env 文件 > 默认配置**==

#### 认识代码块
##### 1. 自动加载器：`tradingagents/__init__.py`
这个文件在系统启动时自动运行，它<mark style="background:#d3f8b6">会直接读取 `.env` 文件。</mark>
后面所有的代码都能直接读取这些配置，就像读取系统自带的变量一样。

##### 2. 配置大本营：`tradingagents/default_config.py`
这个文件是配置的“总司令部”，<mark style="background:#d3f8b6">定义了所有可用的配置项和它们的默认值</mark>。
在系统启动时会检查系统中是否存在环境变量；如果存在，就用环境变量的值替换原先设定的默认值

##### 3. 运行时配置器：`tradingagents/dataflows/config.py`
这个文件是配置的“实时管家”，负责在程序运行时提供和更新配置。
<font color="#92d050">能够获取当前的配置</font>

## Chapter 2: LLM客户端与模型适配层

#### **LLM客户端：**
1. 为什么需要：不同的LLM调用请求的时候，API参数不同。<font color="#92d050">需要一个适配器，针对不同AI模型的API 格式，系统能自动适配</font>
2. 含义：LLM客户端是一个智能路由系统，里面包含了各个LLM的客户端内容。能够根据提供的 `provider` 参数，决定创建哪个客户端。
	每个客户端都有自己的 `get_llm()` 方法，负责配置和返回一个可用的 AI 模型实例。它知道自己的模型需要哪些特殊的零件（参数），并自动组装好。

#### 模型适配层
模型适配层具有以下的能力：
1. 模型能力表：<font color="#92d050">知道每个模型的“特长”</font>——有些模型支持 **工具调用**、有些模型支持 **结构化输出**、有些模型支持 **思考模式**
	含义：是一本“模型说明书”。系统在调用模型之前，会先查一下这本说明书，知道这个模型能做什么、不能做什么。
2. <font color="#92d050">处理不同模型的特殊之处</font>
	例子：DeepSeek 的推理模型有一个特殊要求：如果它返回了 `reasoning_content`（思考内容），你在下一轮对话中必须把它发回去，否则模型会报错！
	MiniMax 的 M2.x 推理模型默认会把思考内容（`<think>...</think>` 标签）直接混在回复里。这样会污染你的报告。适配层会自动添加一个特殊参数，让思考内容单独存放在一个字段里
3. 处理推理参数——不同的模型对“思考深度”有不同的叫法——适配层会处理这些差异
	例子：**Google** 叫 `thinking_level`（思考级别）
	 **OpenAI** 叫 `reasoning_effort`（推理努力程度）
	 **Anthropic** 叫 `effort`（努力程度）
4. 确保模型名称正确——适配层会检查拼写是否正确
5. 统一回复格式——不同模型返回的内容格式不同，适配层能够统一回复内容
6. 自动读取API 密钥环境变量——在 `.env` 文件中设置对应的环境变量，适配层会自动读取

## Chapter 3: 数据流与供应商路由

含义：能够知道从哪里获得数据；如果获取数据的路径受阻，可以换路径；把不同格式的数据处理成统一的格式

#### 数据管道
能力：
1. <font color="#92d050">自动获取数据</font>——发出请求后，路由系统自动找到合适的数据源
2. <font color="#92d050">自动选择数据源</font>——系统内部记录了每种数据可以从哪些数据源获取
3. <font color="#92d050">自动更换数据源</font>——如果第一家数据源失败，自动尝试下一个
	==配置数据源偏好==——通过配置文件实现；还可以==为不同的数据类型设置不同的数据源==
4. <font color="#92d050">符号标准化</font>——将不同的符号转化成数据源能看懂的名字
5. <font color="#92d050">数据泄露防护</font>——控制数据的选择（在数据质量检查这个环节实现）

总结数据管道全流程
接收用户请求——>符号标准化——>供应商路由（获取、选择、更换数据源）——>调用具体数据源——>数据清洗与格式统一——>数据质量检查——>返回给 Agent 使用

角色：假如你是一名经验丰富的AI程序员，有丰富的产品经历，能够熟练构建skill
目标：我现在希望构建一套用于产品从需求产出到工程上线的全过程的skill，需要对每个skill的输入和输出进行强制要求 下面的是具体的文件的路径，希望你根据这个模板一步步进行skill的构建。
/Users/aa00158/ai-product-skills/skill切分项目清单.md 这个我和mentor多轮确定后的具体需求，整个skill构建的方向应该朝着这部分实现
/Users/aa00158/ai-product-skills/AI 产品技能全景 视频文字稿（21 讲）.md' 这个是原来的文件
/Users/aa00158/ai-product-skills/项目背景与构建约定.md 这个是项目的背景框架 我希望你在后续构建skill的时候，一定要尊重任务背景记录表中的框架，但是具体内容一定要落在AI 产品技能全景视频文字稿和skill切分项目清单上
目前的阶段目标是，已经构建完前8个skill，第9个skill的方案也设计出来了。我希望你根据前面列出来的这些文档，仔细研究第9个skill的构建方案，重点看以下几点：
    1. 是否符合mentor的需求，方向是否正确
    2. 内容是否正确，schema等内容是否符合要求
    3. 是否有过度工程化的问题
/Users/aa00158/Documents/Codex/2026-07-08/ai-skill-skill-skill-skill-users-4/outputs/第9个Skill具体构建方案.md

